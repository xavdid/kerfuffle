extends ./layout.jade

block content
  .response(ng-show="res")
    h3
      | How about 
      strong.orange {{res.volumeInfo.title}} 
      | by 
      span.darkgreen
        | {{res.volumeInfo.authors[0]}}
      | ?
    .row
      .col-sm-8
        p.text-justify
          | {{res.volumeInfo.description}} [
          span.purple
            | {{res.volumeInfo.categories[0]}}
          | ] [
          span.darkgreen
            | {{res.volumeInfo.publishedDate.substring(0, 4)}}
          | ] 
      .col-sm-4
        img.img-responsive.center-block.cover(src="{{gbooksImage(res.id)}}")