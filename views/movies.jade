extends ./layout.jade

block content
  h1 
    | So you're looking for a 
    span.blue
      | {{media_type.slice(0, -1)}}
    | .
    i.fa.fa-refresh.spaced(ng-class="{true: 'fa-spin'}[loading]" ng-click="refresh()")
  .response(ng-show="res")
    h3
      | How about 
      strong.orange {{res.movie.title}}
      | ?
    .row
      .col-sm-8
        p.text-justify
          | {{res.movie.overview}} [
          span.darkgreen
            | {{res.movie.year}}
          | ] 
      .col-sm-4
        img.img-responsive.center-block(src="{{res.movie.images.poster.thumb}}")